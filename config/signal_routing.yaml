# Signal routing table — maps signal_type to action metadata.
# Used by action_composer.py to convert DomainSignal → ActionProposal.
# Fallback: _FALLBACK_SIGNAL_ROUTING loaded from this YAML at import time by action_composer.py.
#
# Fields per entry:
#   action_type:     The action to compose (must be in _ALLOWED_ACTION_TYPES)
#   friction:        User friction level: low | standard | high
#   min_trust:       Minimum trust level required to auto-execute (0–2)
#   reversible:      Whether the action can be undone
#   undo_window_sec: Seconds to undo (null if irreversible)
#   status:          Route lifecycle: active (has a producer) | stub (Phase 2 forward-declaration)
#
# Ref: specs/action.md Phase 4

# Content / Social (DEBT-SIG-001: was silently dropped — no route existed)
content_stale:
  action_type: instruction_sheet
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]
  description: "Content posting gap exceeds threshold — prompt a post draft"
  producer: config/patterns.yaml PAT-PR-001 PAT-PR-002

# Finance
bill_due:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

financial_alert:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
property_tax_due:
  action_type: instruction_sheet
  friction: high
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

subscription_renewal:
  action_type: instruction_sheet
  friction: low
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
medical_bill_high:
  action_type: email_send
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 30
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

# Calendar
# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
calendar_conflict:
  action_type: calendar_modify
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 300
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

appointment_needed:
  action_type: email_send
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 30
  status: active
  allowed_sources: [deterministic]

# Email
# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
email_needs_reply:
  action_type: email_reply
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 30
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

# Social
# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
birthday_approaching:
  action_type: whatsapp_send
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD (priority P0 — implement occasion_tracker.py skill)

# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
birthday_in_7d:
  action_type: whatsapp_send
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD (priority P0 — implement occasion_tracker.py skill)

# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
life_event_detected:
  action_type: email_send
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 30
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

# Kids
# DEBT-SIG-002/SIG-003: missing_assignment route unreachable — email_signal_extractor
# emits school_action_needed for all school emails; no separate producer exists.
# Downgraded to stub. school_action_needed covers this case.
# RD-25: Keeping stub to prevent accidental re-introduction of a dead route.
# Do NOT activate until a dedicated school skill exists that emits this signal.
missing_assignment:
  action_type: email_send
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 30
  status: stub
  allowed_sources: [deterministic]
  # RD-25: no dedicated producer; school_action_needed covers this case.
  # Activate only when a dedicated school skill is built.
  producer: TBD (school_action_needed covers this — revisit when dedicated school skill built)

# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
school_event_rsvp:
  action_type: email_reply
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 30
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

# Immigration
# DEBT-SIG-002: No producer confirmed — downgraded from active to stub (priority P0 — high impact)
immigration_deadline:
  action_type: email_send
  friction: high
  min_trust: 1
  reversible: true
  undo_window_sec: 30
  status: stub
  allowed_sources: [deterministic]
  producer: TBD (priority P0 — implement immigration_monitor.py skill)

# Health
# DEBT-SIG-002: No producer confirmed — downgraded from active to stub (priority P0 — health)
prescription_refill:
  action_type: instruction_sheet
  friction: standard
  min_trust: 2
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD (priority P0)

# Vehicle
# DEBT-SIG-002: No producer confirmed — downgraded from active to stub (priority P1)
vehicle_recall:
  action_type: instruction_sheet
  friction: standard
  min_trust: 2
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD (priority P1)

# Insurance
insurance_renewal:
  action_type: instruction_sheet
  friction: low
  min_trust: 2
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# Open Items
# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
open_item_aging:
  action_type: reminder_create
  friction: low
  min_trust: 2
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

open_item_overdue:
  action_type: reminder_create
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# Generic calendar creation
# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
event_invitation:
  action_type: calendar_create
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 300
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

# IoT / Home (ARTHA-IOT Wave 1+2)
device_offline:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

security_device_offline:
  action_type: instruction_sheet
  friction: high
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

energy_anomaly:
  action_type: instruction_sheet
  friction: low
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

supply_low:
  action_type: instruction_sheet
  friction: low
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

spa_maintenance:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# Home Intelligence skill signals (Part 1: motion triage, Part 2: anomaly scan)
motion_alert:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

temperature_anomaly:
  action_type: instruction_sheet
  friction: low
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

camera_offline:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

presence_anomaly:
  action_type: instruction_sheet
  friction: low
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
security_travel_conflict:
  action_type: instruction_sheet
  friction: high
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

# E1 — Email Signal Extractor
event_rsvp_needed:
  action_type: email_reply
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 30
  status: active
  allowed_sources: [deterministic]

appointment_confirmed:
  action_type: calendar_create
  friction: low
  min_trust: 1
  reversible: true
  undo_window_sec: 300
  status: active
  allowed_sources: [deterministic]

delivery_arriving:
  action_type: reminder_create
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

security_alert:
  action_type: instruction_sheet
  friction: high
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

school_action_needed:
  action_type: reminder_create
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

form_deadline:
  action_type: instruction_sheet
  friction: high
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# E12 — Decision Tracker (RD-33: restored — decision_tracker.py consumer exists)
decision_detected:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]
  producer: pipeline.py Step 8d AI reasoning
  notes: >-
    Captured by DecisionTracker.capture_from_signal() — never auto-writes;
    user must confirm via instruction_sheet proposal before persistence.

# E6 — Subscription Lifecycle (extended)
# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
subscription_trial_ending:
  action_type: instruction_sheet
  friction: low
  min_trust: 2
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

subscription_renewal_upcoming:
  action_type: instruction_sheet
  friction: low
  min_trust: 2
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

subscription_cancellation_deadline:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

subscription_annual_review:
  action_type: instruction_sheet
  friction: low
  min_trust: 2
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# E3 — Pattern Engine signals
goal_stale:
  action_type: instruction_sheet
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

relationship_stale:
  action_type: instruction_sheet
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

health_alert:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

maintenance_due:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

insurance_renewal_due:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# E9 — Attachment Router
document_financial:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

document_medical:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

document_immigration:
  action_type: instruction_sheet
  friction: high
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

document_school:
  action_type: instruction_sheet
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

document_insurance:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

document_property:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# Career Search (specs/career-ops.md §3.1 Phase 1, FR-CS-4)
# interview_scheduled: friction=standard per TS-2 (escalated from low in v1.5.0)
career_eval_complete:
  action_type: instruction_sheet
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]

# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
career_interview_scheduled:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
career_offer_received:
  action_type: instruction_sheet
  friction: high
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

# DEBT-SIG-002: No producer confirmed — downgraded from active to stub
career_offer_deadline:
  action_type: instruction_sheet
  friction: high
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]
  producer: TBD

career_application_rejected:
  action_type: instruction_sheet
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]

# Phase 2 — portal scanner signals (CareerSearchAgent)
new_portal_match:
  action_type: instruction_sheet
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: stub
  allowed_sources: [deterministic]

# DEBT-008: orphaned signals — no routing existed before; now routed
automation_failure:
  action_type: instruction_sheet
  friction: standard
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

goal_autopark_candidate:
  action_type: todo_sync
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

slack_action_item:
  action_type: todo_sync
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]

# RD-09: slack_after_hours — activated (was stub). Boundary protection signal.
# Uses instruction_sheet (not reminder_create) to match the pattern of RD-01/07.
# friction: low — surfaces to user as a low-friction proposal requiring review.
slack_after_hours:
  action_type: instruction_sheet
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]
  producer: pipeline.py (timestamp-based, not pattern-based)
  notes: >-
    Work-life boundary protection. Slack message received outside
    work_hours.start/end from user_profile.yaml. friction: low so boundary
    violations surface immediately without requiring extra approval steps.

# RD-33: Content signals — previously absent from routing, silently discarded.
content_moment_missed:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]
  producer: config/patterns.yaml (content moment detection)
  notes: >-
    Triggered when a content opportunity window passes without a post.
    Surfaces as instruction_sheet proposal for review.

content_staged_unreviewed:
  action_type: instruction_sheet
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]
  producer: config/patterns.yaml (staged content monitor)
  notes: >-
    Triggered when content has been staged but not reviewed within the
    expected review window. friction: low — review reminder only.

# RD-33: Address update workflow notification signal.
address_update_notification:
  action_type: email_send
  friction: standard
  min_trust: 1
  reversible: true
  undo_window_sec: 30
  status: active
  allowed_sources: [deterministic]
  producer: scripts/action_composer.py _workflow_address_change()
  notes: >-
    Emitted for each contact in an address change workflow. Routes to
    email_send handler to draft address update notification emails.

# ── Canonical signal types (Initiative 6, Phase 4 consolidation) ─────────────
# These catch-all routes handle DomainSignal objects with canonical signal_type
# but no subtype set (e.g., from non-extractor producers). The extractor always
# populates subtype, so action_composer routes via subtype first.
deadline:
  action_type: instruction_sheet
  friction: standard
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]
  description: "Catch-all: any deadline-class signal without a specific subtype route"

confirmation:
  action_type: reminder_create
  friction: low
  min_trust: 0
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]
  description: "Catch-all: any confirmation-class signal without a specific subtype route"

security:
  action_type: instruction_sheet
  friction: high
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]
  description: "Catch-all: any security-class signal without a specific subtype route"

informational:
  action_type: instruction_sheet
  friction: low
  min_trust: 1
  reversible: false
  undo_window_sec: null
  status: active
  allowed_sources: [deterministic]
  description: "Catch-all: any informational-class signal without a specific subtype route"
